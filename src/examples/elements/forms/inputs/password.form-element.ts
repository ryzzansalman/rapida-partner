import type { IFormInput } from "../../../../interfaces/form-input.interface";

export const password: IFormInput = {
    type: "input",
    dataType: "password",
    label: "Senha",
    name: "password",
}